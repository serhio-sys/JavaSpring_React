server:
  port: 7000
spring:
  datasource:
    url: jdbc:postgresql://db:5432/StudentsAPI
    username: wertun
    password: 43554453
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect